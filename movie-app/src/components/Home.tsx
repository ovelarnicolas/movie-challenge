import { FC } from "react";

const Home: FC<any> = () => {
  return (
    <div className="container">
      <header className="jumbotron">
        <h3>React 18 + Typescript + NetCore 6 + MongoDB</h3>
        <p>This is a web app using those technologies.</p>
      </header>
    </div>
  );
};

export default Home;
